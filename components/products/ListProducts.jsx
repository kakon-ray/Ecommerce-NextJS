import React from 'react';

const ListProducts = ({data}) => {
    console.log(data)
    return (
        <div>
            <h1>Product List</h1>
        </div>
    );
};

export default ListProducts;